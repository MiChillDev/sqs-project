{
  "name": "sqs-e2e",
  "private": true,
  "packageManager": "pnpm@10.16.0",
  "type": "module",
  "scripts": {
    "generate-api-types": "cd ../../api && pnpm generate",
    "typecheck": "tsc --noEmit",
    "seed": "./run tsx seed/seed-cli.ts",
    "test": "./run playwright test",
    "test:headed": "./run playwright test --headed",
    "test:ui": "./run playwright test --ui",
    "test:debug": "./run playwright test --debug",
    "test:ci": "pnpm typecheck && pnpm seed && pnpm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
